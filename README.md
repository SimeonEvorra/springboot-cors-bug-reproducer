# springboot-cors-bug-reproducer

Build and launch test CorsTest to reproduce issue (the test fails on "testRepositoryForbiddenCors")

Change spring boot version in POM to 2.5.4 and the test will be successful